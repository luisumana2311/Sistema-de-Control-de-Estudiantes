# PostgreSQL (producción y desarrollo con PostgreSQL)
DATABASE_URL=postgresql+psycopg://student_app:replace_me@localhost:5432/student_control

# Desarrollo local sin PostgreSQL (opcional)
# DATABASE_URL=sqlite:///./student_control.db

# En Railway, referencia la variable DATABASE_URL del servicio PostgreSQL.
# No agregues contraseñas reales a este archivo.
